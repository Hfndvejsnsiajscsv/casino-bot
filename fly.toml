app = "casino-bot-fkp-lq"
primary_region = "mad"

[build]
  dockerfile = "Dockerfile"

[processes]
  app = "python botprueba.py"

[deploy]
  strategy = "immediate"

[[vm]]
  memory = "256mb"
  cpu_kind = "shared"
  cpus = 1